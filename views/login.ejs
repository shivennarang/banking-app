<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <link rel="preconnect" href="https://fonts.gstatic.com/" crossorigin="" />
    <link
      rel="stylesheet"
      as="style"
      onload="this.rel='stylesheet'"
      href="https://fonts.googleapis.com/css2?display=swap&amp;family=Noto+Sans%3Awght%40400%3B500%3B700%3B900&amp;family=Work+Sans%3Awght%40400%3B500%3B700%3B900"
    />
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
    <style>
        .error {
            color: #ef4444; /* Tailwind red-500 */
            font-size: small;
            margin-top: 10px;
            margin-bottom: 10px;
        }
    </style>
</head>
<body class="bg-[#111a22] font-sans text-white">
    <%- include('partials/header') %>
    <div class="flex justify-center items-center mt-16">
        <div class="flex flex-col justify-around items-start gap-8 shadow-lg w-[30%] rounded-xl p-8 bg-[#1a2632] border border-[#344d65]">
            <div class="text-3xl font-extrabold">Login to AccountManager</div>
            
            <form action="/login" method="POST" class="flex flex-col gap-5 w-full">
                <!-- Email -->
                <div class="flex flex-col gap-1 w-full">
                    <label for="email" class="text-lg font-bold">Email:</label>
                    <input 
                        type="email" 
                        id="email" 
                        name="email" 
                        required
                        class="form-input w-full rounded-xl border border-[#344d65] bg-[#1a2632] text-white placeholder:text-[#93adc8] p-3 text-base font-normal leading-normal focus:outline-none focus:border-[#344d65]"
                    />
                </div>
        
                <!-- Password -->
                <div class="flex flex-col gap-1 w-full">
                    <label for="password" class="text-lg font-bold">Password:</label>
                    <input 
                        type="password" 
                        id="password" 
                        name="password" 
                        required
                        class="form-input w-full rounded-xl border border-[#344d65] bg-[#1a2632] text-white placeholder:text-[#93adc8] p-3 text-base font-normal leading-normal focus:outline-none focus:border-[#344d65]"
                    />
                </div>
                
                <% if (errorMessage && errorMessage.includes('password')) { %>
                    <div class="error"><%= errorMessage %><a href="/register" style=" color: white;"> or Register.</a></div>
                <% } %>
                
              
                <div>
                    <button 
                        type="submit"
                        class="w-full bg-[#1980e6] text-white font-bold py-3 px-4 rounded-xl hover:bg-[#0d5a9f] transition-colors"
                    >
                        Login
                    </button>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
