<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="preconnect" href="https://fonts.gstatic.com/" crossorigin="" />
  <link
    rel="stylesheet"
    as="style"
    onload="this.rel='stylesheet'"
    href="https://fonts.googleapis.com/css2?display=swap&amp;family=Inter%3Awght%40400%3B500%3B700%3B900&amp;family=Noto+Sans%3Awght%40400%3B500%3B700%3B900"
  />
  <title>Update Transaction</title>
  <style>
    body {
      font-family: 'Inter', 'Noto Sans', sans-serif;
      background-color: #111418;
      overflow-x: hidden;
    }
    .header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 12px 40px;
      border-bottom: 1px solid #293038;
    }
    .header .logo {
      display: flex;
      align-items: center;
      gap: 16px;
      color: white;
    }
    .header .logo svg {
      width: 48px;
      height: 48px;
    }
    .header h2 {
      color: white;
      font-size: 18px;
      font-weight: 700;
      line-height: 1.25;
      letter-spacing: -0.015em;
    }
    .main-content {
      display: flex;
      justify-content: center;
      padding: 20px 80px;
    }
    .form-container {
      display: flex;
      flex-direction: column;
      width: 100%;
      max-width: 960px;
      padding: 20px;
      background-color: #111418;
    }
    .form-container h3 {
      color: white;
      font-size: 24px;
      font-weight: 700;
      line-height: 1.25;
      padding: 20px 16px 8px;
    }
    .form-container label {
      display: flex;
      flex-direction: column;
      min-width: 160px;
      margin-bottom: 12px;
    }
    .form-container p {
      color: white;
      font-size: 16px;
      font-weight: 500;
      line-height: 1.5;
      margin-bottom: 8px;
    }
    .form-container input, .form-container select {
      width: 100%;
      height: 56px;
      padding: 0 16px;
      border-radius: 12px;
      background-color: #293038;
      color: white;
      font-size: 16px;
      font-weight: 400;
      line-height: 1.5;
      border: none;
      outline: none;
    }
    .form-container input::placeholder, .form-container select::placeholder {
      color: #9dabb8;
    }
    .form-container button {
      display: flex;
      align-items: center;
      justify-content: center;
      min-width: 84px;
      max-width: 480px;
      height: 48px;
      padding: 0 20px;
      border-radius: 12px;
      font-size: 16px;
      font-weight: 700;
      line-height: 1.2;
      cursor: pointer;
      background-color: #1980e6;
      color: white;
      border: none;
      outline: none;
    }
  </style>
</head>
<body>
  <%- include('partials/navbar', { userName: name }) %>
 
  <main class="main-content">
    <div class="form-container">
      <h3>Update Transaction</h3>
      <form action="/transactions/<%= transaction._id %>/update" method="POST">
        
        <label for="account_id">
          <p>Account ID:</p>
          <input type="text" id="account_id" name="account_id" value="<%= transaction.account_id %>" required />
        </label>
        <label for="type">
            <p>Transaction Type:</p>
            <select id="type" name="type" required>
              <option value="" disabled>Select transaction type</option>
              <option value="Deposit" <%= transaction.transaction_type === 'Deposit' ? 'selected' : '' %>>Deposit</option>
              <option value="Withdrawal" <%= transaction.transaction_type === 'Withdrawal' ? 'selected' : '' %>>Withdrawal</option>
              <option value="Transfer" <%= transaction.transaction_type === 'Transfer' ? 'selected' : '' %>>Transfer</option>
            </select>
          </label>
        <label for="amount">
          <p>Amount:</p>
          <input type="number" id="amount" name="amount" value="<%= transaction.amount %>" step="0.01" required />
        </label>
       
       
        <label for="description">
          <p>Description:</p>
          <input type="text" id="description" name="description" value="<%= transaction.description %>" placeholder="Enter description" />
        </label>
        <button type="submit">Update Transaction</button>
      </form>
    </div>
  </main>
</body>
</html>
